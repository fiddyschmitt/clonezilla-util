using CommandLine;
using System;
using System.Collections.Generic;
using System.Text;

namespace clonezilla_util.CL.Verbs
{
    public class BaseVerb
    {
        //[Option('c', "use-cache", HelpText = "Use a cache so that subsequent access is much faster. First run will be slow though.", Required = false)]
        //public bool UseCache { get; set; }
    }
}
